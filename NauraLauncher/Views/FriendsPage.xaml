<UserControl x:Class="NauraLauncher.Views.FriendsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ctrl="clr-namespace:NauraLauncher.Controls"
             xmlns:vm="clr-namespace:NauraLauncher.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="880" d:DesignWidth="1504"
             d:DataContext="{d:DesignInstance Type=vm:FriendsViewModel}">

    <Grid Margin="28,8,28,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel VerticalAlignment="Center" Margin="0,0,24,0">
                    <TextBlock Text="FRIENDS &amp; VOICE" Style="{StaticResource Label}" Foreground="{DynamicResource SubtleForegroundBrush}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                        <TextBlock Text="{Binding FriendsCount}" Style="{StaticResource CardHeader}" Margin="0,0,12,0"/>
                        <Border Style="{StaticResource Badge.Accent}" Padding="8,4" VerticalAlignment="Center">
                            <TextBlock Text="{Binding OnlineCount}" FontWeight="Bold" FontSize="11" Foreground="{DynamicResource AccentBrush}"/>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="20" Width="260" Height="40" Margin="0,0,12,0">
                    <Grid Margin="14,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ctrl:IconControl Grid.Column="0" Data="{StaticResource Icon.Search}" Brush="{DynamicResource MutedForegroundBrush}" Width="16" Height="16" Margin="0,0,10,0"/>
                        <Grid Grid.Column="1">
                            <TextBox x:Name="SearchBox" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" FontSize="13"/>
                            <TextBlock Text="Search heroes..." IsHitTestVisible="False" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource SubtleForegroundBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource Label}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Grid>
                </Border>
                <Button Style="{StaticResource Button.Primary}" Height="40" Padding="18,0" Command="{Binding SearchCommand}">
                    <TextBlock Text="SEARCH" FontWeight="Bold" FontSize="12"/>
                </Button>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="20" Height="40" Padding="0,0,14,0" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                        <TextBox x:Name="AddFriendBox" Text="{Binding AddFriendUsername, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Width="160" VerticalContentAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" FontSize="13"/>
                        <TextBlock Text="Add friend..." IsHitTestVisible="False" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource SubtleForegroundBrush}" Margin="-160,0,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource Label}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=AddFriendBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>
                <Button Style="{StaticResource Button.Accent}" Height="40" Padding="18,0" Command="{Binding AddFriendCommand}">
                    <StackPanel Orientation="Horizontal">
                        <ctrl:IconControl Data="{StaticResource Icon.UserPlus}" Brush="#FF06120C" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="ADD FRIEND" FontWeight="Bold" FontSize="12"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource Button.Icon}" Width="40" Height="40" Margin="12,0,0,0" Command="{Binding RefreshCommand}">
                    <ctrl:IconControl Data="{StaticResource Icon.Refresh}" Brush="{DynamicResource ForegroundBrush}" Width="16" Height="16"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Status Message -->
        <Border Grid.Row="0" Background="#2634D399" CornerRadius="8" Padding="12,8" Margin="0,50,0,0" VerticalAlignment="Top" HorizontalAlignment="Center" Visibility="{Binding StatusMessage, Converter={StaticResource StrVis}}">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource AccentBrush}" FontSize="12" FontWeight="Bold"/>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- Friends List -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Online Friends -->
                    <Border Style="{StaticResource Card}" Padding="20,16" Margin="0,0,0,16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <Ellipse Width="8" Height="8" Fill="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="ONLINE FRIENDS" Style="{StaticResource PanelTitle}"/>
                                <TextBlock Text="{Binding OnlineCount}" Style="{StaticResource Label}" Margin="12,0,0,0" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                            </StackPanel>

                            <ItemsControl ItemsSource="{Binding OnlineFriends}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Grid Grid.Column="0">
                                                    <Border Width="44" Height="44" CornerRadius="12">
                                                        <Border.Background>
                                                            <ImageBrush ImageSource="{Binding FriendAvatar}" Stretch="UniformToFill"/>
                                                        </Border.Background>
                                                    </Border>
                                                    <Ellipse Width="12" Height="12" Fill="{DynamicResource AccentBrush}" Stroke="{DynamicResource CardBrush}" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-2,-2"/>
                                                </Grid>

                                                <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding FriendName}" FontWeight="Bold" FontSize="13" Foreground="{DynamicResource ForegroundBrush}"/>
                                                        <Border Style="{StaticResource Badge}" Padding="6,2" Margin="8,0,0,0" Background="{DynamicResource SurfaceBrush}">
                                                            <TextBlock Text="{Binding FriendLevel, StringFormat='Lv.{0}'}" FontSize="10" Foreground="{DynamicResource MutedForegroundBrush}"/>
                                                        </Border>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                        <TextBlock Text="{Binding FriendClass}" Style="{StaticResource Label}" FontSize="11" Foreground="{DynamicResource MutedForegroundBrush}"/>
                                                        <TextBlock Text="• Twin City" Style="{StaticResource Label}" FontSize="11" Foreground="{DynamicResource SubtleForegroundBrush}" Margin="6,0,0,0"/>
                                                    </StackPanel>
                                                </StackPanel>

                                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Button Style="{StaticResource Button.Icon}" Width="36" Height="36" Margin="0,0,8,0" ToolTip="Voice Call" Command="{Binding DataContext.StartVoiceCallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                        <ctrl:IconControl Data="{StaticResource Icon.Phone}" Brush="{DynamicResource AccentBrush}" Width="16" Height="16"/>
                                                    </Button>
                                                    <Button Style="{StaticResource Button.Icon}" Width="36" Height="36" ToolTip="Remove Friend" Command="{Binding DataContext.RemoveFriendCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                        <ctrl:IconControl Data="{StaticResource Icon.UserMinus}" Brush="{DynamicResource MutedForegroundBrush}" Width="16" Height="16"/>
                                                    </Button>
                                                    <Button Style="{StaticResource Button.Icon}" Width="36" Height="36" ToolTip="Block" Command="{Binding DataContext.BlockUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                        <ctrl:IconControl Data="{StaticResource Icon.Block}" Brush="{DynamicResource DestructiveBrush}" Width="16" Height="16"/>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- All Friends -->
                    <Border Style="{StaticResource Card}" Padding="20,16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <ctrl:IconControl Data="{StaticResource Icon.Users}" Brush="{DynamicResource ForegroundBrush}" Width="18" Height="18" Margin="0,0,10,0"/>
                                <TextBlock Text="ALL FRIENDS" Style="{StaticResource PanelTitle}"/>
                            </StackPanel>

                            <ItemsControl ItemsSource="{Binding Friends}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" Width="44" Height="44" CornerRadius="12" Opacity="0.7">
                                                    <Border.Background>
                                                        <ImageBrush ImageSource="{Binding FriendAvatar}" Stretch="UniformToFill"/>
                                                    </Border.Background>
                                                </Border>

                                                <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding FriendName}" FontWeight="Bold" FontSize="13" Foreground="{DynamicResource ForegroundBrush}"/>
                                                    <TextBlock Text="{Binding FriendClass}" Style="{StaticResource Label}" FontSize="11" Foreground="{DynamicResource SubtleForegroundBrush}" Margin="0,2,0,0"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Style="{StaticResource Badge}" Padding="8,4" Background="{DynamicResource SurfaceBrush}" Margin="0,0,8,0">
                                                        <TextBlock Text="OFFLINE" FontSize="10" Foreground="{DynamicResource SubtleForegroundBrush}"/>
                                                    </Border>
                                                    <Button Style="{StaticResource Button.Icon}" Width="32" Height="32" Command="{Binding DataContext.RemoveFriendCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                        <ctrl:IconControl Data="{StaticResource Icon.UserMinus}" Brush="{DynamicResource MutedForegroundBrush}" Width="14" Height="14"/>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Right Panel: Requests, Blocked, Voice -->
            <StackPanel Grid.Column="2">
                <!-- Active Voice Call -->
                <Border Style="{StaticResource Card}" Padding="20" Margin="0,0,0,16" Background="#2634D399" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" Visibility="{Binding ActiveCall, Converter={StaticResource BoolVis}}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Ellipse Width="8" Height="8" Fill="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="VOICE CALL ACTIVE" Style="{StaticResource Label}" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding ActiveCall.CalleeName}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                        <TextBlock Text="Connected • 00:42" Style="{StaticResource Label}" Foreground="{DynamicResource MutedForegroundBrush}" Margin="0,4,0,12"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Style="{StaticResource Button.Outline}" Height="40" Margin="0,0,6,0">
                                <ctrl:IconControl Data="{StaticResource Icon.Mic}" Brush="{DynamicResource ForegroundBrush}" Width="16" Height="16"/>
                            </Button>
                            <Button Grid.Column="1" Style="{StaticResource Button.Primary}" Height="40" Margin="6,0,0,0" Background="{DynamicResource DestructiveBrush}" Command="{Binding DataContext.EndActiveCallCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <TextBlock Text="END CALL" FontWeight="Bold" FontSize="11" Foreground="White"/>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Pending Requests -->
                <Border Style="{StaticResource Card}" Padding="20,16" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <ctrl:IconControl Data="{StaticResource Icon.Bell}" Brush="{DynamicResource ForegroundBrush}" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="FRIEND REQUESTS" Style="{StaticResource PanelTitle}"/>
                            <Border Style="{StaticResource Badge.Accent}" Padding="6,3" Margin="8,0,0,0" Visibility="{Binding PendingRequests.Count, Converter={StaticResource BoolVis}}">
                                <TextBlock Text="{Binding PendingRequests.Count}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                            </Border>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding PendingRequests}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                                <Border Width="32" Height="32" CornerRadius="8" Background="{DynamicResource MutedBrush}">
                                                    <ctrl:IconControl Data="{StaticResource Icon.User}" Brush="{DynamicResource MutedForegroundBrush}" Width="16" Height="16"/>
                                                </Border>
                                                <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding FromUsername}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource ForegroundBrush}"/>
                                                    <TextBlock Text="{Binding SentAt, StringFormat='{}{0:HH:mm}'}" Style="{StaticResource Label}" FontSize="10"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                                                <Button Style="{StaticResource Button.Primary}" Height="32" Padding="14,0" Margin="0,0,8,0" Command="{Binding DataContext.AcceptRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                    <TextBlock Text="ACCEPT" FontSize="11" FontWeight="Bold"/>
                                                </Button>
                                                <Button Style="{StaticResource Button.Outline}" Height="32" Padding="14,0" Command="{Binding DataContext.DeclineRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                    <TextBlock Text="DECLINE" FontSize="11" Foreground="{DynamicResource ForegroundBrush}"/>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="No pending requests" Style="{StaticResource Label}" Foreground="{DynamicResource SubtleForegroundBrush}" HorizontalAlignment="Center" Margin="0,12,0,0" Visibility="{Binding PendingRequests.Count, Converter={StaticResource BoolVis}, ConverterParameter=invert}"/>
                    </StackPanel>
                </Border>

                <!-- Blocked Users -->
                <Border Style="{StaticResource Card}" Padding="20,16" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <ctrl:IconControl Data="{StaticResource Icon.Block}" Brush="{DynamicResource DestructiveBrush}" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="BLOCKED PLAYERS" Style="{StaticResource PanelTitle}"/>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding BlockedUsers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding BlockedName}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource ForegroundBrush}"/>
                                                <TextBlock Text="{Binding BlockedAt, StringFormat='Blocked {0:MM/dd}'}" Style="{StaticResource Label}" FontSize="10" Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Style="{StaticResource Button.Ghost}" Padding="10,6" Command="{Binding DataContext.UnblockUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                <TextBlock Text="UNBLOCK" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="No blocked players" Style="{StaticResource Label}" Foreground="{DynamicResource SubtleForegroundBrush}" HorizontalAlignment="Center" Margin="0,12,0,0" Visibility="{Binding BlockedUsers.Count, Converter={StaticResource BoolVis}, ConverterParameter=invert}"/>
                    </StackPanel>
                </Border>

                <!-- Search Results -->
                <Border Style="{StaticResource Card}" Padding="20,16" Visibility="{Binding SearchResults.Count, Converter={StaticResource BoolVis}}">
                    <StackPanel>
                        <TextBlock Text="SEARCH RESULTS" Style="{StaticResource PanelTitle}" Margin="0,0,0,12"/>
                        <ItemsControl ItemsSource="{Binding SearchResults}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FriendName}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource ForegroundBrush}"/>
                                                <TextBlock Text="{Binding FriendClass}" Style="{StaticResource Label}" FontSize="10"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Style="{StaticResource Button.Primary}" Height="32" Padding="12,0" Command="{Binding DataContext.AddFriendCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                <TextBlock Text="ADD" FontSize="11" FontWeight="Bold"/>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
